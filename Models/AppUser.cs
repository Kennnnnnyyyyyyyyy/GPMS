using Microsoft.AspNetCore.Identity;

namespace Gate_Pass_management.Models
{
    // Extend later with profile fields (e.g., DisplayName, Department)
    public class AppUser : IdentityUser
    {
    }
}
